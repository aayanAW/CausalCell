# Core
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
anndata>=0.10
scanpy>=1.10
pandas>=2.0
networkx>=3.0
matplotlib>=3.7
seaborn>=0.12
h5py>=3.8
requests>=2.28

# Causal discovery
gies>=0.0.3

# Testing
pytest>=7.0

# Optional: inspre (R package, install via Rscript)
# Rscript -e 'remotes::install_github("brielin/inspre")'

# Optional: model packages (install separately, may have GPU/version requirements)
# pip install cpa-tools          # CPA
# pip install torch_geometric    # Required before cell-gears
# pip install cell-gears         # GEARS
# pip install geneformer         # Geneformer
